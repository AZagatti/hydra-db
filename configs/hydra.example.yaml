hydra:
  name: hydra
  version: "0.1.0"
  log_level: info

gateway:
  host: localhost
  port: 8080
  read_timeout: 30
  write_timeout: 30

policy:
  default_budget: 10000
  rate_limit: 100

logging:
  level: info
  format: json
