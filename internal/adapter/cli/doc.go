// Package cli implements the CLI adapter for Hydra.
//
// It translates between command-line input and the internal Envelope format,
// producing human-readable output on the reverse path.
package cli
