// Package http implements the HTTP adapter for Hydra.
//
// It translates between HTTP JSON request/response bodies and the internal
// Envelope format, acting as the bridge for RESTful API consumers.
package http
