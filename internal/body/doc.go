// Package body provides the shared core types and primitives for Hydra.
//
// The body is what makes Hydra one organism instead of six random packages.
// All heads share the same identity model, tracing, event bus, configuration,
// and health reporting through the types defined in this package.
package body
