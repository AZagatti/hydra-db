// Package agent implements the Agent Runtime head of Hydra.
//
// The agent runtime is the thinking/acting head. It creates, manages,
// and executes agents with structured context, tool attachment, retry
// logic, and timeout enforcement.
package agent
